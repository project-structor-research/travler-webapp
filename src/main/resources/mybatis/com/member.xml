<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
	<select id="list" parameterType="hashmap" resultType="com.springframework.travler.models.Member">
		SELECT
			SEQ,
			USERNAME,
			PASSWORD,
			NAME,
			EMAIL,
			ADDRESS
		FROM 
			MEMBER
		WHERE
			USERNAME = #{P_USERNAME}
	</select>
</mapper>